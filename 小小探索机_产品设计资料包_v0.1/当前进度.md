# 当前实机进度

更新日期：2026-06-09

## 已完成

- 已取得 Waveshare RP2040-Zero 主控板。
- 已确认 USB-C 连接正常。
- 按下 BOOT/RESET 后，电脑可识别 `RPI-RP2` 磁盘。
- 已核对主板丝印：板边 `0` 至 `29` 为 GPIO 编号，板载 WS2812 数据引脚为
  GPIO16。
- Arduino IDE 已切换为中文界面。
- 已添加 Arduino-Pico 开发板管理器地址：
  `https://github.com/earlephilhower/arduino-pico/releases/download/global/package_rp2040_index.json`
- 因 Arduino IDE 直连 GitHub 超时，已通过浏览器下载工具链压缩包，并放入
  Arduino 的 `staging/packages` 缓存目录。

## 正在进行

- 在开发板管理器中安装
  `Raspberry Pi Pico/RP2040/RP2350 by Earle F. Philhower`。

## 下一步

1. 确认开发板包安装成功。
2. 选择 `Waveshare RP2040 Zero`。
3. 安装 `Adafruit NeoPixel by Adafruit`。
4. 上传
   `firmware/board_bringup_test/board_bringup_test.ino`。
5. 验证板载 RGB 依次显示红、绿、蓝并熄灭。
6. 以 115200 波特率确认串口输出
   `RP2040-Zero board test: OK`。

RGB 与串口验证目前尚未完成。
